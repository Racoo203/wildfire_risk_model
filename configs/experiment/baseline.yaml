preprocessing:
  force_recompute: true

seasons:
  active: ["summer"]

labels:
  density_method: "convolution"
  classify_method: "gmm"

modeling:
  mlflow_experiment: "wildfire-summer-jenks-baseline"
  models: ["random_forest", "xgboost", "neural_net"]